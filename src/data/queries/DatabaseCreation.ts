/**
 * Creates the necessary tables in the database.
 * @returns A promise that resolves when the tables are created.
 */
export async function CreateTables(query: Function): Promise<void> {
    // add the table creation scripts here (e.g. query('CREATE TABLE ...'))
    // you may want to create a function for each table to access the query object
}
